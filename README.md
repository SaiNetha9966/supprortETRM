
  # Web Pages with React

  This is a code bundle for Web Pages with React. The original project is available at https://www.figma.com/design/OCZg1v9jdcyWpul1OOYzV9/Web-Pages-with-React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  